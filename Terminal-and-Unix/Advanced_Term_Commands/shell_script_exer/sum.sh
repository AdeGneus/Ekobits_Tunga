# Enter the two numbers with space between, don't include the + sign else it throws an error

## To run the script and see the output, type the folder path and include the two numbers you want to sum
echo "The sum of $1 and $2 is:" $(($1+$2))
